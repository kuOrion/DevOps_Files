[Unit]
Description=ERP16 daily backup, all real clients (independent of deploys)
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
User=ubuntu
ExecStart=/home/ubuntu/DevOps_Files/scripts/deploy.sh backup-all
